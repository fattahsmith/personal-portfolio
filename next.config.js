/** @type {import('next').NextConfig} */

const nextConfig = {
  /* config options here */
  outputFileTracingRoot: process.cwd(),
  // Hapus semua konfigurasi khusus untuk menggunakan default Next.js
};

module.exports = nextConfig;
